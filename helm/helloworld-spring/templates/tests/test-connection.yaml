apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helloworld-spring.fullname" . }}-test-connection"
  labels:
    {{- include "helloworld-spring.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helloworld-spring.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
